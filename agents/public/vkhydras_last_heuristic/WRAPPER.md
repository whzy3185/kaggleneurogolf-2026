# Wrapper Notes: vkhydras_last_heuristic

This directory exposes a public repository source as `main.py`.

Wrapper action: copied standalone Python source from
`vkhydras/orbit-wars-heuristic-bots`.

Known risks:

- File may contain local assumptions from the author's submission history.
- It is used as a local benchmark opponent only.
- Static copy only; local tournament results are not official scores.

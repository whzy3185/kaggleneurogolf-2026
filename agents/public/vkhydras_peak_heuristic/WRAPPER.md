# Wrapper Notes: vkhydras_peak_heuristic

This directory exposes a public repository source as `main.py` with `def agent(obs, config=None)`.

Wrapper action: copied standalone Python source from `vkhydras/orbit-wars-heuristic-bots`.

Known risks:

- File may contain local assumptions from the author's submission history.
- Static copy only; see smoke results in `reports/PUBLIC_AGENT_LOADING.md`.
